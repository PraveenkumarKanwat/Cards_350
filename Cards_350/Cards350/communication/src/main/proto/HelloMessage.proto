syntax="proto3";
package com.bugchod.communication;

message HelloRequest{
    string message_send = 1;
}

message HelloResponse{
    string message_receive = 2;
}

service HelloMessage{
    rpc send(HelloRequest) returns (HelloResponse);
}
